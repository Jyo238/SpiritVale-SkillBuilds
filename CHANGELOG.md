# CHANGELOG

## v1.1.0（2026-08-16）

新增兩個勾選項，並修兩個玩家回報的 bug。

**新功能：時裝與外觀也能跟著 Build 走**

- 編輯面板多兩個勾選：**時裝（含坐騎／寵物）**、**外觀（長相）**
  - 時裝＝衣櫃裡套用的一切：外裝、武器外觀、坐騎、寵物、光環／拖尾特效、稱號、徽章…
    走遊戲衣櫃「套用」同一條路（`ApplyCosmetic` / `RemoveCosmetic`），只能套用衣櫃裡已擁有的；
    武器外觀會像衣櫃一樣先檢查跟手上武器類型相容，不相容就跳過並列出
  - 外觀＝人物長相十項（膚色／髮型／髮色／眉／鬍／嘴／眼／眼色／耳／瞳），
    走衣櫃「外觀」頁籤／造型師 NPC 的同一條路（`ApplyAppearance`），免費、無次數限制
- **兩者新 Build 預設不勾**（cfg「2.新組預設勾選」可改）：打扮通常是「人」的不是「流派」的，
  預設勾上的話，存完 Build 之後換的坐騎、改的髮型會在還原時被靜靜換回去。
  想每組各有一套打扮的人，右鍵該組打勾即可——**儲存永遠全記，勾選只管還原**，不用重存
- 還原順序變成：技能點 → 能力點 → 裝備 → **時裝／外觀** → 快捷列
- 「卸下快照沒有的裝備欄」勾選現在也管時裝欄（快照當時沒騎坐騎，還原就會把坐騎收起來）
- 舊版存的 Build 沒有時裝／外觀資料，這兩類會整段跳過（不會把你的打扮清掉），還原完成時會明講。
  補資料**不用整組重存**：右鍵那組 →「**把目前的時裝／外觀存進這組（其他不動）**」，
  技能／裝備／能力點／快捷列一個都不會動，並順手把時裝／外觀勾起來

**防手滑（升級後第一天就撞到的）**

- **Shift+左鍵覆寫既有 Build 現在也要按兩次確認**。它是「把你**目前身上**的技能／裝備／打扮整組蓋掉那組」，
  剛還原完 A、想幫 B 補個時裝就 Shift+點 B，B 的點法就變成 A 的了——實測第一天就有人這樣把兩組混成一組。
  只想更新打扮請走上面那顆按鈕
- **被覆寫／刪除的舊 Build 會自動備份**到同資料夾的 `local.spiritvale.skillbuilds.presets.backup.json`
  （每角色留最近 30 筆，含時間與原因）。要救回來：關遊戲，把那筆的 `Preset` 物件貼回
  `presets.json` 裡對應角色的位置即可（純 JSON）

**追查中：切 Build 後魔導書「替換」效果沒接上（例如 Elementalist 的 Elemental Attunement 變回 Wind Attunement，要拔掉重裝才好）**

- 加了狀態元件快照 log：還原開始前／技能點套用後／換裝階段結束／完成／完成後 2 秒各印一行
  （技能替換表、狀態替換表、調諧、畫面上的技能／狀態顯示、武器元素、魔導書），**Ctrl+Shift+D** 隨時可手動印
- cfg「3.診斷 / 換裝後重掛魔導書」（實驗，預設關）：換裝階段結束後把身上的魔導書逐本卸下再裝回
  ＝自動做你手動那兩下；根因查清楚後會拿掉

**修正**

- **修正「換飾品失敗」**（常見於左手戴著唯一手套、兩組 Build 只差右手飾品）。
  根因是遊戲 `ApplyEquip` 的欄位規則：飾品**兩欄都滿時一律換左欄**，右欄永遠不會被直接換掉；
  唯一（Unique）飾品則一律進左欄。舊版直接對著右欄送「穿」，結果遊戲把左手的手套擠回背包、
  補裝重試每一輪都在左欄來回擠，永遠收斂不了。
  現在依遊戲規則**先模擬再送**：卸不要的 → 唯一件先穿 → 一般件補進空欄、兩欄都滿就先讓出
  不要的那欄；飾品改以「兩件都在身上」為到位（左右由遊戲決定，唯一件反正只能在左）。
  正常情況一輪到位，總驗收＋補裝重試仍保留兜底
- **修正編輯面板的「刪除」按鈕點不動**：點擊命中判定漏列了刪除鈕的名字，
  點下去一路往上找到面板本體就回傳，永遠對不上「刪除」
- 送出每件換裝前先驗收，前一件的連鎖效應已經讓這件到位就直接跳過（少送 RPC，
  也不會出現「已在身上→再送→伺服器忽略→逾時」的假失敗）
- **換裝監控 log 常駐**（不用開診斷模式）：每次還原印出換裝佇列、飾品規劃（現況→目標→動作）；
  逾時時印出該物品實際位置（背包／身上哪一欄）與兩個飾品欄的當下狀態。
  回報換裝問題時把 `BepInEx\LogOutput.log` 裡 `[Build]` 開頭的那幾行貼上來即可

## v1.0.1（2026-08-12）

實機驗證後的兩處修正，功能與 v1.0.0 相同。

- **還原摘要現在一定會報換裝件數**。之前只有「完全成功」時才顯示，
  一旦有任何缺漏（例如能力點需要調降）就只列缺了什麼、不列做成了什麼，
  會讓人以為根本沒換裝
- **修正多輪補裝時換裝件數少算**：改用第一輪算出的差異件數，不再拿重試清單的長度
- **啟動時清掉舊版遺留的設定鍵**。從舊版升級的人，cfg 裡會同時出現
  「素質點」和「能力點」兩個鍵而困惑（插件本來就只讀後者，純顯示問題）

**升級注意**：既有 Build 的勾選設定不會被新的預設值改寫——這是刻意的，
避免靜默推翻你手動改過的設定。從舊版升上來的人，`能力點` 勾選仍是開著的，
要關請右鍵該組 Build 取消勾選。

## v1.0.0（2026-08-11）首次發布

把「技能點＋能力點＋裝備／神器／魔導書＋快捷列」存成 Build，一鍵還原。

**功能**

- 技能視窗右上角的 Build 按鈕列：Shift+左鍵存、左鍵點兩次還原、右鍵編輯、「＋」加一組（最多 12）
- 熱鍵 `Ctrl+F1~F6` 還原、`Ctrl+Shift+F1~F6` 儲存（不用開技能視窗）
- 還原順序：套用技能點 → 配能力點 → 換裝 → 綁快捷列，畫面顯示每一步進度
- 每組獨立勾選要還原哪幾類（技能點／快捷列／能力點／裝備／神器／魔導書／卸下多餘裝備）。
  儲存永遠全記，勾選只管還原 → 可做出「只勾裝備」的純換裝組
- 編輯面板可改名與刪除（刪除需點兩次確認，刪後往前遞補）
- Build 依角色分開存；設定「存檔資料夾」指向雲端資料夾即可多台電腦共用

**只做玩家自己做得到的事**

- **不呼叫任何「重置」**：重置技能／能力點是 Waybinder NPC 專屬功能，交給玩家自己去按
- **技能點**直接送出最終清單——這正是你右鍵退點、再按「套用」走的同一條路，所以隨地可用。
  而且是原子操作：伺服器整包接受或整包拒絕，**失敗時你原本的點法完全不會被動到**
- **能力點只加不減**（遊戲介面本來就只有 `+`）。Build 若需要調降某項能力點，
  會跳過能力點、其餘照常還原，並提示你先去 Waybinder 重置後再還原一次
- 新存的 Build **預設不勾能力點**——加下去就得跑 NPC 才能重來，預設不動比較安全

**穩定性**

- 每一步都對本地資料驗收後才前進（技能等級全對、能力點六項全對、裝備 uid 相符），
  不是送出就當成功
- 換裝與快捷列都有總驗收＋自動重試（各 2 輪）：
  飾品左右欄由遊戲決定，第一輪可能放錯邊，第二輪自然歸位
- 戰鬥中禁止換裝（伺服器限制）→ 還原前先擋，不會換到一半失敗
- 能力點不足會算給你看差幾點（需要 N、剩餘 M、還差 K），不是丟一句「未被接受」
- 缺件（賣掉／交易掉／需求不符）明確列出，其餘照做完
- 舊快照（沒有裝備／能力點資料）還原時自動跳過該類，不會把身上的東西卸掉
- 啟動時反射驗證 RPC 簽名，遊戲改版不符時停用還原並提示，不會崩潰

**相容性**

- BepInEx 6.0.0-be.785（IL2CPP win-x64），遊戲 Unity 6000.0.64f1
- 與繁中翻譯包、紫星販賣保護、交易所比價、詞條快篩 HUD 共存
