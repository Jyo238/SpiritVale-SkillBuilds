# CHANGELOG

## v1.0.1（2026-08-12）

實機驗證後的兩處修正，功能與 v1.0.0 相同。

- **還原摘要現在一定會報換裝件數**。之前只有「完全成功」時才顯示，
  一旦有任何缺漏（例如能力點需要調降）就只列缺了什麼、不列做成了什麼，
  會讓人以為根本沒換裝
- **修正多輪補裝時換裝件數少算**：改用第一輪算出的差異件數，不再拿重試清單的長度
- **啟動時清掉舊版遺留的設定鍵**。從舊版升級的人，cfg 裡會同時出現
  「素質點」和「能力點」兩個鍵而困惑（插件本來就只讀後者，純顯示問題）

**升級注意**：既有 Build 的勾選設定不會被新的預設值改寫——這是刻意的，
避免靜默推翻你手動改過的設定。從舊版升上來的人，`能力點` 勾選仍是開著的，
要關請右鍵該組 Build 取消勾選。

## v1.0.0（2026-08-11）首次發布

把「技能點＋能力點＋裝備／神器／魔導書＋快捷列」存成 Build，一鍵還原。

**功能**

- 技能視窗右上角的 Build 按鈕列：Shift+左鍵存、左鍵點兩次還原、右鍵編輯、「＋」加一組（最多 12）
- 熱鍵 `Ctrl+F1~F6` 還原、`Ctrl+Shift+F1~F6` 儲存（不用開技能視窗）
- 還原順序：套用技能點 → 配能力點 → 換裝 → 綁快捷列，畫面顯示每一步進度
- 每組獨立勾選要還原哪幾類（技能點／快捷列／能力點／裝備／神器／魔導書／卸下多餘裝備）。
  儲存永遠全記，勾選只管還原 → 可做出「只勾裝備」的純換裝組
- 編輯面板可改名與刪除（刪除需點兩次確認，刪後往前遞補）
- Build 依角色分開存；設定「存檔資料夾」指向雲端資料夾即可多台電腦共用

**只做玩家自己做得到的事**

- **不呼叫任何「重置」**：重置技能／能力點是 Waybinder NPC 專屬功能，交給玩家自己去按
- **技能點**直接送出最終清單——這正是你右鍵退點、再按「套用」走的同一條路，所以隨地可用。
  而且是原子操作：伺服器整包接受或整包拒絕，**失敗時你原本的點法完全不會被動到**
- **能力點只加不減**（遊戲介面本來就只有 `+`）。Build 若需要調降某項能力點，
  會跳過能力點、其餘照常還原，並提示你先去 Waybinder 重置後再還原一次
- 新存的 Build **預設不勾能力點**——加下去就得跑 NPC 才能重來，預設不動比較安全

**穩定性**

- 每一步都對本地資料驗收後才前進（技能等級全對、能力點六項全對、裝備 uid 相符），
  不是送出就當成功
- 換裝與快捷列都有總驗收＋自動重試（各 2 輪）：
  飾品左右欄由遊戲決定，第一輪可能放錯邊，第二輪自然歸位
- 戰鬥中禁止換裝（伺服器限制）→ 還原前先擋，不會換到一半失敗
- 能力點不足會算給你看差幾點（需要 N、剩餘 M、還差 K），不是丟一句「未被接受」
- 缺件（賣掉／交易掉／需求不符）明確列出，其餘照做完
- 舊快照（沒有裝備／能力點資料）還原時自動跳過該類，不會把身上的東西卸掉
- 啟動時反射驗證 RPC 簽名，遊戲改版不符時停用還原並提示，不會崩潰

**相容性**

- BepInEx 6.0.0-be.785（IL2CPP win-x64），遊戲 Unity 6000.0.64f1
- 與繁中翻譯包、紫星販賣保護、交易所比價、詞條快篩 HUD 共存
